name: test
on: 
  workflow_call:
    inputs:
      environment:
        required: true
        type: string

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: big step
        run: echo "_reusable_test.yml - environment is ${{ inputs.environment }}"
